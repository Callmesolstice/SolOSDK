"""Shared HTTP session with retry and backoff."""

from __future__ import annotations

import requests


def get_session() -> requests.Session:
    """Shared HTTP session with retry + backoff. TODO: implement."""
    raise NotImplementedError
